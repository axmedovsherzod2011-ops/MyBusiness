{
  "name": "marketplace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @marketplace/web --filter @marketplace/api dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @marketplace/api start",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
